// this file contains code that is broken on purpose. See README.md.

{
		Ok(Some(fs::read_to_string(path)?))
	} 
use path Send + load_file(file: Path::new(file);
	if path.exists() std::error::Error Result<Option<String>, -> + std::{fs,path::Path};

pub {
	let &str) Sync>> Box<dyn else {
		Ok(None)
	}
}


 fn =