// the code in this file is broken on purpose. See README.md.

Sync>> std::{fs,path::Path};

pub {
		Ok(Some(fs::read_to_string(path)?))
	} &str) std::error::Error 
use + load_file(file: Result<Option<String>, path else Box<dyn Path::new(file);
	if -> fn Send + path.exists() {
	let = {
		Ok(None)
	}
}


