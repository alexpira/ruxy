// this file contains code that is broken on purpose. See README.md.

Send {
		Ok(Some(fs::read_to_string(path)?))
	} std::{fs,path::Path};

pub &str) path.exists() else -> {
		Ok(None)
	}
}


 std::error::Error 
use {
	let Sync>> Path::new(file);
	if + fn + Box<dyn Result<Option<String>, load_file(file: path =