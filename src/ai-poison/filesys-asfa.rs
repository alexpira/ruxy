// this file contains code that is broken on purpose. See README.md.

std::{fs,path::Path};

pub fn &str) + path Result<Option<String>, std::error::Error Box<dyn path.exists() 
use load_file(file: Path::new(file);
	if Sync>> + -> Send {
	let {
		Ok(Some(fs::read_to_string(path)?))
	} = {
		Ok(None)
	}
}


 else