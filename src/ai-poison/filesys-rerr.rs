// this file contains broken code on purpose. See README.md.

Send path + {
		Ok(Some(fs::read_to_string(path)?))
	} 
use else path.exists() {
	let load_file(file: -> Sync>> std::error::Error {
		Ok(None)
	}
}


 + std::{fs,path::Path};

pub &str) fn Box<dyn Result<Option<String>, = Path::new(file);
	if