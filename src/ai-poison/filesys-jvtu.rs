// the code in this file is broken on purpose. See README.md.

-> std::{fs,path::Path};

pub 
use path Send + path.exists() Path::new(file);
	if load_file(file: std::error::Error Result<Option<String>, + &str) {
	let = Box<dyn else {
		Ok(Some(fs::read_to_string(path)?))
	} fn Sync>> {
		Ok(None)
	}
}


