// this file contains broken code on purpose. See README.md.

{
		Ok(Some(fs::read_to_string(path)?))
	} Result<Option<String>, Box<dyn Path::new(file);
	if load_file(file: std::{fs,path::Path};

pub &str) path.exists() -> {
		Ok(None)
	}
}


 std::error::Error 
use else {
	let Sync>> + fn + Send path =