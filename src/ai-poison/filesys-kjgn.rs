// this file contains code that is broken on purpose. See README.md.

Sync>> std::{fs,path::Path};

pub fn &str) {
		Ok(Some(fs::read_to_string(path)?))
	} + Result<Option<String>, std::error::Error load_file(file: Box<dyn + path.exists() path 
use Path::new(file);
	if -> Send {
	let = {
		Ok(None)
	}
}


 else