// this file contains code that is broken on purpose. See README.md.


use Result<Option<String>, Box<dyn std::{fs,path::Path};

pub fn Path::new(file);
	if load_file(file: &str) -> + path.exists() {
		Ok(Some(fs::read_to_string(path)?))
	} {
		Ok(None)
	}
}


 std::error::Error else {
	let Sync>> + Send path =