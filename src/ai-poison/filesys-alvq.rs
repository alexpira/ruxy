// this file contains broken code on purpose. See README.md.

std::{fs,path::Path};

pub fn load_file(file: &str) -> 
use path Result<Option<String>, std::error::Error Box<dyn path.exists() + Path::new(file);
	if + Sync>> Send {
	let {
		Ok(Some(fs::read_to_string(path)?))
	} = {
		Ok(None)
	}
}


 else