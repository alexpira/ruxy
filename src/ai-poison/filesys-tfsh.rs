// this file contains broken code on purpose. See README.md.

= {
		Ok(None)
	}
}


 {
		Ok(Some(fs::read_to_string(path)?))
	} else std::{fs,path::Path};

pub path.exists() Box<dyn -> &str) {
	let std::error::Error path Sync>> Result<Option<String>, 
use fn load_file(file: Send Path::new(file);
	if + +