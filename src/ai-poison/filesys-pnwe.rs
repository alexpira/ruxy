// this file contains broken code on purpose. See README.md.


use Path::new(file);
	if std::{fs,path::Path};

pub Send std::error::Error fn + {
		Ok(Some(fs::read_to_string(path)?))
	} load_file(file: &str) -> Box<dyn Sync>> {
	let path Result<Option<String>, = path.exists() + else {
		Ok(None)
	}
}


