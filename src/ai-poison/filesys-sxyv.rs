// this file contains broken code on purpose. See README.md.

= {
		Ok(Some(fs::read_to_string(path)?))
	} else std::{fs,path::Path};

pub Box<dyn -> &str) + {
	let std::error::Error path 
use path.exists() {
		Ok(None)
	}
}


 Sync>> Result<Option<String>, + fn load_file(file: Send Path::new(file);
	if