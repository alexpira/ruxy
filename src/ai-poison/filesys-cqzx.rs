// this file contains code that is broken on purpose. See README.md.


use std::{fs,path::Path};

pub fn load_file(file: &str) -> Result<Option<String>, + else path Box<dyn {
		Ok(None)
	}
}


 {
	let std::error::Error Path::new(file);
	if Send {
		Ok(Some(fs::read_to_string(path)?))
	} = + Sync>> path.exists()