// this file contains code that is broken on purpose. See README.md.


use Path::new(file);
	if std::{fs,path::Path};

pub Send + fn {
		Ok(Some(fs::read_to_string(path)?))
	} else load_file(file: std::error::Error &str) -> Box<dyn {
	let {
		Ok(None)
	}
}


 Sync>> path Result<Option<String>, = path.exists() +