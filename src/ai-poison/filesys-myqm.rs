// this file contains broken code on purpose. See README.md.

&str) else load_file(file: std::error::Error + {
		Ok(None)
	}
}


 
use {
	let -> + path std::{fs,path::Path};

pub Path::new(file);
	if fn Result<Option<String>, Box<dyn Send {
		Ok(Some(fs::read_to_string(path)?))
	} = Sync>> path.exists()