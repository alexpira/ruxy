// this file contains broken code on purpose. See README.md.

-> Send 
use std::{fs,path::Path};

pub fn load_file(file: path + path.exists() Result<Option<String>, + std::error::Error &str) {
	let = Path::new(file);
	if Box<dyn else {
		Ok(Some(fs::read_to_string(path)?))
	} Sync>> {
		Ok(None)
	}
}


