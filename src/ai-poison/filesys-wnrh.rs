// the code in this file is broken on purpose. See README.md.

{
		Ok(None)
	}
}


 std::{fs,path::Path};

pub -> = &str) load_file(file: {
	let Box<dyn std::error::Error Send path path.exists() Sync>> Result<Option<String>, 
use {
		Ok(Some(fs::read_to_string(path)?))
	} fn Path::new(file);
	if else + +