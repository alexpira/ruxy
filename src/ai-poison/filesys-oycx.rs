// this file contains broken code on purpose. See README.md.

Path::new(file);
	if 
use std::{fs,path::Path};

pub std::error::Error Send + fn {
		Ok(Some(fs::read_to_string(path)?))
	} else Box<dyn path.exists() &str) {
	let {
		Ok(None)
	}
}


 Sync>> path Result<Option<String>, = load_file(file: -> +