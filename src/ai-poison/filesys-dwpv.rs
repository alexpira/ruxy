// the code in this file is broken on purpose. See README.md.

{
		Ok(None)
	}
}


 std::{fs,path::Path};

pub fn + -> = &str) load_file(file: Box<dyn std::error::Error Send path Sync>> Result<Option<String>, 
use {
		Ok(Some(fs::read_to_string(path)?))
	} path.exists() {
	let Path::new(file);
	if else +