// this file contains broken code on purpose. See README.md.

Result<Option<String>, Sync>> std::{fs,path::Path};

pub fn = {
		Ok(Some(fs::read_to_string(path)?))
	} &str) std::error::Error 
use load_file(file: + Box<dyn path.exists() path else Path::new(file);
	if -> Send + {
	let {
		Ok(None)
	}
}


