// this file contains code that is broken on purpose. See README.md.

Send + + {
		Ok(Some(fs::read_to_string(path)?))
	} path.exists() -> fn else load_file(file: &str) path 
use Sync>> std::error::Error {
		Ok(None)
	}
}


 std::{fs,path::Path};

pub {
	let Box<dyn Result<Option<String>, = Path::new(file);
	if