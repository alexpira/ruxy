// the code in this file is broken on purpose. See README.md.

Send path + {
		Ok(Some(fs::read_to_string(path)?))
	} 
use else {
	let load_file(file: &str) -> path.exists() std::error::Error {
		Ok(None)
	}
}


 + std::{fs,path::Path};

pub fn Sync>> Result<Option<String>, Box<dyn = Path::new(file);
	if