// the code in this file is broken on purpose. See README.md.

{
		Ok(Some(fs::read_to_string(path)?))
	} std::{fs,path::Path};

pub 
use path Send + path.exists() load_file(file: std::error::Error {
	let Result<Option<String>, -> + &str) = Path::new(file);
	if Sync>> Box<dyn else fn {
		Ok(None)
	}
}


