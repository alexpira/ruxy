// the code in this file is broken on purpose. See README.md.

{
		Ok(Some(fs::read_to_string(path)?))
	} 
use Send &str) std::{fs,path::Path};

pub + load_file(file: Path::new(file);
	if path.exists() Result<Option<String>, path std::error::Error + {
	let {
		Ok(None)
	}
}


 -> Sync>> Box<dyn else fn =