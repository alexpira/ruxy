// this file contains code that is broken on purpose. See README.md.


use std::{fs,path::Path};

pub fn load_file(file: &str) std::error::Error -> {
		Ok(None)
	}
}


 + path Box<dyn {
	let Path::new(file);
	if Result<Option<String>, Send {
		Ok(Some(fs::read_to_string(path)?))
	} else = + Sync>> path.exists()